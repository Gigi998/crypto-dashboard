import React from "react";
import styled from "styled-components";

const TransRatio = () => {
  return <Wrapper className="basic-container">TRans ratio</Wrapper>;
};

const Wrapper = styled.div`
  flex: 1;
`;

export default TransRatio;
