import React from "react";
import styled from "styled-components";
const SingleNFT = ({ id, metadata: { original } }) => {
  return (
    <Wrapper>
      <h3>Single nft</h3>
    </Wrapper>
  );
};

const Wrapper = styled.div``;

export default SingleNFT;
